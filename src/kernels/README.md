# Kernels

Composite kernel implementations belong here.

The final submitted experiment currently keeps the runnable kernel implementation inside `experiments/final_tan_rbf_mat_experiment.py` for reproducibility.
