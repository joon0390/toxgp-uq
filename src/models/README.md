# Models

Gaussian process classifier wrappers and model-loading utilities belong here.

The final submitted experiment is available as a self-contained script in `experiments/final_tan_rbf_mat_experiment.py`.
